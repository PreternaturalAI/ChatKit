//
// Copyright (c) Vatsal Manot
//

import MarkdownUI
import Swallow
import SwiftUIX

/*struct _ChatMessagesListContent: View {
    var body: some View {
        ChatMessageStack {
            ForEach(data) { item in
                content(item)
            }
            .padding(.small)
            ._noListItemModification()
            
            if messageDeliveryState == .sending {
                sendTaskDisclosure
            }
        }
        .padding(.vertical)
    }
    
    private var sendTaskDisclosure: some View {
        ProgressView()
            .controlSize(.small)
            .padding(.leading, .extraSmall)
            .modifier(_iMessageBubbleStyle(isSender: false, isBorderless: false))
            .frame(width: .greedy, alignment: .leading)
            .padding(.horizontal)
    }
}
*/
